# Authentication Architecture Overview

Visual representation of how authentication works in the Rent Management System.

---

## 🏗️ **System Architecture**

```
┌─────────────────────────────────────────────────────────────────┐
│                         FRONTEND (React)                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Pages:                    Hooks:              Components:       │
│  • /login                  • useAuth()         • LinkedAccounts  │
│  • /register               • useIdentities()                     │
│  • /forgot-password                                              │
│  • /reset-password         Utils:                                │
│  • /auth-callback          • lib/auth.ts                         │
│  • /dashboard              • lib/authUtils.ts                    │
│                            • lib/supabase.ts                     │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         │ HTTP/HTTPS
                         │ (cookies, JSON)
                         │
┌────────────────────────┴────────────────────────────────────────┐
│                  BACKEND (Vercel Serverless)                     │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Main API:             Middleware:                               │
│  • /api/auth.ts        • verify-auth.ts                          │
│    - login             • rate-limit.ts                           │
│    - register                                                    │
│    - google oauth                                                │
│    - forgot-password                                             │
│    - reset-password                                              │
│    - link-google                                                 │
│    - get user                                                    │
│    - get identities                                              │
│                                                                  │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         │ Supabase SDK
                         │
┌────────────────────────┴────────────────────────────────────────┐
│                      SUPABASE (Auth + DB)                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Auth Service:          Database:                                │
│  • auth.users           • public.users (synced)                  │
│  • auth.identities      • properties                             │
│  • JWT management       • tenants                                │
│  • OAuth providers      • leases                                 │
│  • Password reset       • payments                               │
│  • Email service        • documents                              │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 **Authentication Flows**

### **1. Email/Password Login Flow**

```
┌──────────┐
│  User    │
└────┬─────┘
     │
     │ 1. Enter email & password
     ▼
┌─────────────────┐
│  /login page    │
└────┬────────────┘
     │
     │ 2. POST /api/auth?action=login
     ▼
┌──────────────────────┐
│  /api/auth.ts        │
│  (login handler)     │
└────┬─────────────────┘
     │
     │ 3. supabase.auth.signInWithPassword()
     ▼
┌─────────────────────┐
│  Supabase Auth      │
└────┬────────────────┘
     │
     │ 4. Validate credentials
     │ 5. Return JWT + user
     ▼
┌──────────────────────┐
│  /api/auth.ts        │
│  (set cookie)        │
└────┬─────────────────┘
     │
     │ 6. Set-Cookie: supabase-auth-token
     ▼
┌─────────────────┐
│  Frontend       │
│  (redirect to   │
│   /dashboard)   │
└─────────────────┘
```

---

### **2. Google OAuth Flow**

```
┌──────────┐
│  User    │
└────┬─────┘
     │
     │ 1. Click "Sign in with Google"
     ▼
┌─────────────────┐
│  /login page    │
└────┬────────────┘
     │
     │ 2. Redirect to /api/auth?action=google
     ▼
┌──────────────────────┐
│  /api/auth.ts        │
│  (google handler)    │
└────┬─────────────────┘
     │
     │ 3. supabase.auth.signInWithOAuth({ provider: 'google' })
     ▼
┌─────────────────────┐
│  Supabase Auth      │
└────┬────────────────┘
     │
     │ 4. Generate OAuth URL
     │ 5. Redirect to Google
     ▼
┌─────────────────────┐
│  Google OAuth       │
└────┬────────────────┘
     │
     │ 6. User authorizes
     │ 7. Redirect with tokens
     ▼
┌─────────────────────────┐
│  /auth-callback         │
│  (OAuth callback page)  │
└────┬────────────────────┘
     │
     │ 8. Extract tokens from URL hash
     │ 9. POST /api/auth?action=set-session
     ▼
┌──────────────────────┐
│  /api/auth.ts        │
│  (set cookie)        │
└────┬─────────────────┘
     │
     │ 10. Set-Cookie: supabase-auth-token
     │ 11. POST /api/auth?action=sync-user
     ▼
┌─────────────────────┐
│  Supabase DB        │
│  (sync to users)    │
└────┬────────────────┘
     │
     │ 12. User synced
     ▼
┌─────────────────┐
│  Frontend       │
│  (redirect to   │
│   /dashboard)   │
└─────────────────┘
```

---

### **3. Password Reset Flow**

```
┌──────────┐
│  User    │
└────┬─────┘
     │
     │ 1. Click "Forgot Password"
     ▼
┌─────────────────────────┐
│  /forgot-password page  │
└────┬────────────────────┘
     │
     │ 2. Enter email
     │ 3. POST /api/auth?action=forgot-password
     ▼
┌──────────────────────┐
│  /api/auth.ts        │
└────┬─────────────────┘
     │
     │ 4. supabase.auth.resetPasswordForEmail()
     ▼
┌─────────────────────┐
│  Supabase Auth      │
└────┬────────────────┘
     │
     │ 5. Send email with reset link
     ▼
┌─────────────────────┐
│  User's Email       │
└────┬────────────────┘
     │
     │ 6. User clicks link
     │    /reset-password#type=recovery&access_token=...
     ▼
┌─────────────────────────┐
│  /reset-password page   │
└────┬────────────────────┘
     │
     │ 7. Validate recovery token
     │ 8. User enters new password
     │ 9. supabase.auth.updateUser({ password })
     ▼
┌─────────────────────┐
│  Supabase Auth      │
└────┬────────────────┘
     │
     │ 10. Password updated
     ▼
┌─────────────────────────┐
│  /reset-password page   │
│  (sign out session)     │
└────┬────────────────────┘
     │
     │ 11. supabase.auth.signOut()
     │ 12. Redirect to /login?success=password-reset
     ▼
┌─────────────────┐
│  /login page    │
│  (show success) │
└─────────────────┘
```

---

### **4. Account Linking Flow** 🆕

```
┌──────────┐
│  User    │
│ (logged  │
│   in)    │
└────┬─────┘
     │
     │ 1. Navigate to Profile
     ▼
┌─────────────────────────┐
│  Dashboard > Profile    │
│  (Linked Accounts)      │
└────┬────────────────────┘
     │
     │ 2. Click "Link Google"
     ▼
┌─────────────────────────────┐
│  LinkedAccountsSection.tsx  │
└────┬────────────────────────┘
     │
     │ 3. supabase.auth.linkIdentity({ provider: 'google' })
     ▼
┌─────────────────────┐
│  Supabase Auth      │
└────┬────────────────┘
     │
     │ 4. Generate OAuth URL (linking mode)
     │ 5. Redirect to Google
     ▼
┌─────────────────────┐
│  Google OAuth       │
└────┬────────────────┘
     │
     │ 6. User authorizes (same email required)
     │ 7. Redirect back with tokens
     ▼
┌─────────────────────────┐
│  /auth-callback         │
└────┬────────────────────┘
     │
     │ 8. Supabase links Google identity to user
     ▼
┌─────────────────────┐
│  Supabase Auth      │
│  (identities table) │
└────┬────────────────┘
     │
     │ 9. Google identity linked
     ▼
┌─────────────────────────┐
│  Dashboard              │
│  (show Google: ✓ Linked)│
└─────────────────────────┘
```

---

## 🔐 **Security Layers**

```
┌─────────────────────────────────────────────────────────────┐
│                        SECURITY LAYERS                       │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  Layer 1: Rate Limiting                                      │
│  ┌────────────────────────────────────────────────────┐    │
│  │ • Login: 5 attempts/min                            │    │
│  │ • Register: 2 attempts/min                         │    │
│  │ • Password Reset: 3 attempts/min                   │    │
│  │ • IP-based tracking                                │    │
│  └────────────────────────────────────────────────────┘    │
│                                                              │
│  Layer 2: Cookie Security                                    │
│  ┌────────────────────────────────────────────────────┐    │
│  │ • HttpOnly (prevents XSS)                          │    │
│  │ • Secure (HTTPS only in production)                │    │
│  │ • SameSite=Lax (CSRF protection)                   │    │
│  │ • 7-day expiration                                 │    │
│  └────────────────────────────────────────────────────┘    │
│                                                              │
│  Layer 3: Input Validation                                   │
│  ┌────────────────────────────────────────────────────┐    │
│  │ • Zod schema validation                            │    │
│  │ • Email format validation                          │    │
│  │ • Password strength requirements                   │    │
│  │ • SQL injection prevention (Supabase ORM)          │    │
│  └────────────────────────────────────────────────────┘    │
│                                                              │
│  Layer 4: JWT Tokens                                         │
│  ┌────────────────────────────────────────────────────┐    │
│  │ • Signed with SUPABASE_JWT_SECRET                  │    │
│  │ • Short expiration (1 hour)                        │    │
│  │ • Refresh token rotation                           │    │
│  │ • Cannot be tampered with                          │    │
│  └────────────────────────────────────────────────────┘    │
│                                                              │
│  Layer 5: Session Management                                 │
│  ┌────────────────────────────────────────────────────┐    │
│  │ • SessionStorage for temporary OAuth data          │    │
│  │ • Auto-clear on tab close                          │    │
│  │ • Recovery sessions auto-expire after use          │    │
│  └────────────────────────────────────────────────────┘    │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 📊 **Data Flow**

### **User Object Journey**

```
1. User registers/logs in
   ↓
2. Supabase creates record in auth.users
   ↓
3. Backend syncs to public.users table
   ↓
4. Backend sets httpOnly cookie with JWT
   ↓
5. Frontend calls /api/auth?action=user
   ↓
6. useAuth() hook provides user data
   ↓
7. Components access user via useAuth()
```

### **Cookie Lifecycle**

```
1. Login/OAuth success
   ↓
2. Backend: Set-Cookie: supabase-auth-token=JWT; HttpOnly; ...
   ↓
3. Browser stores cookie (inaccessible to JavaScript)
   ↓
4. Every API request includes cookie automatically
   ↓
5. Backend validates JWT on protected routes
   ↓
6. Logout: Set-Cookie with Max-Age=0 (expires immediately)
```

---

## 🎯 **Key Files by Function**

### **Authentication Entry Points:**
- `/api/auth.ts` - All auth operations
- `/client/src/hooks/useAuth.ts` - Auth state management

### **OAuth Handling:**
- `/api/auth.ts` (google action)
- `/client/src/pages/auth-callback.tsx`
- `/client/src/lib/supabase.ts`

### **Password Management:**
- `/client/src/pages/forgot-password.tsx`
- `/client/src/pages/reset-password.tsx`

### **Account Linking:**
- `/api/auth.ts` (identities, link-google actions)
- `/client/src/components/LinkedAccountsSection.tsx`
- `/client/src/hooks/useIdentities.ts`

### **Security:**
- `/api/_lib/rate-limit.ts`
- `/api/_lib/verify-auth.ts`
- Cookie configuration in `/api/auth.ts`

---

## 🔧 **Configuration Points**

| Setting | Location | Value |
|---------|----------|-------|
| **Cookie Name** | `/api/auth.ts` | `supabase-auth-token` |
| **Cookie Duration** | `/api/auth.ts` | `604800` (7 days) |
| **Session Storage** | `/client/src/lib/supabase.ts` | `sessionStorage` |
| **OAuth Flow** | `/client/src/lib/supabase.ts` | `implicit` |
| **Rate Limits** | `/api/_lib/rate-limit.ts` | Login: 5/min, Register: 2/min |
| **JWT Secret** | Environment | `SUPABASE_JWT_SECRET` |

---

## 📝 **Quick Reference**

### **To Add New Auth Provider:**
1. Update `/api/auth.ts` - Add provider action
2. Update `/client/src/components/LinkedAccountsSection.tsx` - Add UI
3. Configure in Supabase Dashboard

### **To Change Session Duration:**
1. Modify `Max-Age` in `/api/auth.ts` cookie settings

### **To Add Auth to New Route:**
1. Import `useAuth()` hook
2. Add auth guard:
   ```typescript
   const { isAuthenticated, isLoading } = useAuth();
   if (!isLoading && !isAuthenticated) {
     setLocation('/login');
   }
   ```

### **To Debug Auth Issues:**
1. Check browser cookies (DevTools → Application → Cookies)
2. Check `/api/auth?action=user` response
3. Check Supabase Auth logs
4. Check browser console for errors

---

**Created:** January 2, 2026  
**Architecture:** Frontend (React) → Backend (Vercel) → Supabase  
**Auth Methods:** Email/Password, Google OAuth, Account Linking
